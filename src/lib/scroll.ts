/**
 * Scroll distance at which the hero hands off to the header: the scroll cue
 * fades out and the wordmark fades in at the same point, so they read as one
 * exchange rather than two unrelated effects.
 */
export const SCROLL_REVEAL_AT = 48;
