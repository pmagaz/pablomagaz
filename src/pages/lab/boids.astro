---
import ExperimentHeader from '~/components/lab/ExperimentHeader/ExperimentHeader';
import HowItWorks from '~/components/lab/HowItWorks/HowItWorks';
import BoidsStage from '~/components/lab/experiments/Boids/BoidsStage';
import { findExperiment } from '~/data/experiments';
import BaseLayout from '~/layouts/BaseLayout/BaseLayout.astro';

const experiment = findExperiment('boids')!;
---

<BaseLayout title={experiment.title} description={experiment.excerpt}>
  <article class="pm-experiment">
    <ExperimentHeader experiment={experiment} />

    <div class="pm-container pm-experiment__stage">
      <BoidsStage client:only="react" />
      {experiment.howItWorks && <HowItWorks paragraphs={experiment.howItWorks} />}
    </div>
  </article>
</BaseLayout>

<style>
  .pm-experiment__stage {
    padding-bottom: var(--pm-space-14);
  }
</style>
